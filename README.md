# Fiti Agent

A Topic-Scoped LLM Knowledge CLI. 

Organize your knowledge into explicit, namespaced topics (vaults). Let an LLM automatically maintain a structured wiki inside each vault from the raw notes, links, and documents you ingest.
